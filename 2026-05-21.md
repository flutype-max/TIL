# TIL - 2026-05-22

## 오늘의 학습 개요

Phase 5(하네스 엔지니어링 + OpenClaw) 본격 진행. 하네스 엔지니어링 3대 모듈(가드레일/관측성/피드백루프)을 직접 구현하고, 에이전트 행동 메트릭을 Prometheus로 수집해 Grafana 대시보드로 시각화했다. VM3에 OpenClaw를 설치하고 WSL2의 Ollama를 LLM 백엔드로 연동하는 것까지 완료. 최종 통합 파이프라인 직전 단계까지 진행.

---

## Phase 5: 하네스 엔지니어링 핵심 개념

### 하네스 엔지니어링 3대 요소

① 가드레일 (Guardrail)
- AI 에이전트의 입출력을 기술적으로 제어
- 허용/차단 명령어 목록 관리
- 출력에서 민감 정보 필터링

② 관측성 (Observability)
- 에이전트의 모든 행동을 로그/메트릭으로 기록
- 이상 행동 감지 + 성능 모니터링

③ 피드백 루프 (Feedback Loop)
- 실행 결과 검증
- 실패 시 재시도 → 한계 초과 시 사람에게 에스컬레이션

---

## 5-1. guardrail.py 구현 및 테스트

### 구조

- `BLOCKED_COMMANDS`: 위험 명령어 정규식 목록 (`rm -rf`, `dd if=`, `mkfs.`, `chmod 777`, `curl | bash` 등)
- `ALLOWED_PATTERNS`: 허용 명령어 화이트리스트 (`ls`, `cat`, `kubectl get/describe/logs` 등)
- `check_command()`: 명령어가 차단 패턴에 걸리는지 → 허용 목록에 있는지 검사
- `check_output()`: 실행 결과 출력에서 password=, api_key=, secret= 등 민감 정보 차단

### 테스트 결과

```
test@DESKTOP-8BF8G5A:~/aiops-platform/harness$ python3 agent_runner.py
# ls /tmp → 정상 실행 (출력 표시)
[BLOCKED] rm -rf /tmp/test
[BLOCKED]  차단 패턴: rm\s+-rf
```

- `ls /tmp` → SUCCESS (허용 패턴 통과)
- `rm -rf /tmp/test` → BLOCKED (차단 패턴 즉시 차단)

### 사용 방법

guardrail.py는 단독 실행이 아니라 에이전트 실행 코드(agent_runner.py)에서 import해서 사용. 명령 실행 전 `check_command()`로 검사 → 통과한 명령만 실행 → 실행 후 `check_output()`로 출력 필터링.

---

## 5-2. observability.py + Prometheus 메트릭 export

### JSONL 로깅

- `AgentAction` dataclass로 모든 에이전트 행동 기록
- 필드: timestamp, agent_id, action_type, input, output, status, duration_ms, block_reason
- `record()`: 액션을 agent_actions.jsonl에 한 줄씩 append
- `get_summary()`: total/success/blocked/failed 집계

### Prometheus 메트릭 export (metrics_exporter.py)

```bash
pip install prometheus-client --break-system-packages
```

- 포트 8001로 메트릭 노출 (`start_http_server(8001)`)
- `agent_actions_total{status}`: 액션 수 카운터 (success/blocked/failed)
- `agent_action_duration_ms`: 실행 시간 히스토그램
- JSONL 로그를 15초마다 읽어서 메트릭으로 변환

### 확인

```bash
python3 metrics_exporter.py &
curl http://localhost:8001/metrics
# agent_actions_total{status="success"} 1.0
# agent_actions_total{status="blocked"} 1.0
```

---

## 5-3. feedback_loop.py 구현

### 3단계 동작 흐름

1. 가드레일 체크 → 차단 시 즉시 반환 (blocked)
2. 명령 실행 → 성공 시 로깅 후 반환 (success)
3. 실패 시 지수 백오프(2^n초)로 최대 3회 재시도 → 초과 시 에스컬레이션 (escalated)

### 테스트 결과

```
1. 정상 명령어:
INFO:__main__:[SUCCESS] ls /tmp
{'status': 'success', 'output': '...'}

2. 차단 명령어:
WARNING:guardrail:[BLOCKED] rm -rf /tmp/test
{'status': 'blocked', 'reason': ' 차단 패턴: rm\\s+-rf'}

3. 실패 → 재시도 → 에스컬레이션:
ERROR:__main__:[FAILED] cat /nonexistent/file.txt | 재시도: 1/3
ERROR:__main__:[FAILED] cat /nonexistent/file.txt | 재시도: 2/3
ERROR:__main__:[FAILED] cat /nonexistent/file.txt | 재시도: 3/3
CRITICAL:__main__:[ESCALATION] cat /nonexistent/file.txt 최대 재시도 초과 → 사람에게 알림 필요
{'status': 'escalated', ...}
```

3가지 케이스(성공/차단/에스컬레이션) 모두 정상 동작 확인.

---

## 5-4. Prometheus + Grafana 에이전트 행동 시각화

### Prometheus scrape 연결 (가장 많이 막혔던 부분)

처음에 configmap 패치 방식으로 시도했으나 실패:

```bash
kubectl -n monitoring patch configmap kube-prometheus-stack-prometheus ...
# → Targets에 aiops-agent 안 나타남
```

원인: kube-prometheus-stack은 prometheus-operator로 관리되므로 configmap 직접 패치가 무시됨. operator가 Prometheus CR(Custom Resource)을 기준으로 설정을 다시 생성하기 때문.

해결: `additionalScrapeConfigs`를 Secret으로 만들고 Prometheus CR에 패치:

```bash
# 1. scrape 설정 파일 생성
cat > /tmp/additional-scrape.yaml << 'EOF'
- job_name: 'aiops-agent'
  static_configs:
  - targets: ['172.25.38.4:8001']
    labels:
      service: 'aiops-agent'
EOF

# 2. Secret 생성
kubectl -n monitoring create secret generic additional-scrape-configs \
  --from-file=additional-scrape-configs.yaml=/tmp/additional-scrape.yaml

# 3. Prometheus CR에 패치
kubectl -n monitoring patch prometheus kube-prometheus-stack-prometheus \
  --type merge \
  -p '{"spec":{"additionalScrapeConfigs":{"name":"additional-scrape-configs","key":"additional-scrape-configs.yaml"}}}'
```

확인: Prometheus UI(`localhost:9090`) → Status → Targets → `aiops-agent 1/1 up` 확인.

### Grafana 대시보드 구성 (AIOps Agent Monitor)

3개 패널 구성:

- 액션 현황: `sum(agent_actions_total) by (status)`
- 차단 비율: `rate(agent_actions_total{status="blocked"}[5m])`
- 평균 실행 시간: `rate(agent_action_duration_ms_sum[5m]) / rate(agent_action_duration_ms_count[5m])`

blocked/success가 시계열로 구분되어 표시되는 것 확인.

---

## 5-5. VM3 OpenClaw 설치 + WSL2 Ollama 연동

### 설치 시 선택 항목

- Setup mode → QuickStart
- Model/auth provider → Skip for now (Ollama는 config로 직접 설정)
- Channel → Slack (Socket Mode)
- Slack bot token → Phase 4에서 만든 앱의 Bot User OAuth Token(`xoxb-...`) 사용
- Search provider → Skip for now
- Skills / Hooks → Skip for now
- Hatch → Hatch later (config 먼저 설정 후 실행)

### Slack Bot Token 권한

기존 Incoming Webhook과 별개로 Bot Token이 필요. OAuth & Permissions → Bot Token Scopes에 추가:
- `chat:write`
- `channels:read`
- `app_mentions:read`

권한 추가 후 Reinstall to Workspace 하면 토큰 값은 유지되면서 새 스코프 반영됨.

### PATH 문제

설치 후 `openclaw: command not found` 발생.

```bash
find / -name "openclaw" 2>/dev/null
# → /home/test/.npm-global/bin/openclaw
```

원인: `~/.npm-global/bin`이 PATH에 없음.

해결:
```bash
echo 'export PATH="$HOME/.npm-global/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

### Ollama 연동 설정

```bash
openclaw config set models.providers.ollama.apiKey "ollama-local"
openclaw config set models.providers.ollama.baseUrl "http://172.25.38.4:11434"
openclaw config set agents.defaults.model.primary "ollama/qwen2.5-ctx:14b"
openclaw models auth login --provider ollama   # Ollama mode → Local only 선택
openclaw gateway restart
```

### IP 오타 삽질

`openclaw models auth login` 중 base URL을 `172.26.38.4`로 입력 → "Ollama could not be reached" 에러.

```bash
curl http://172.25.38.4:11434/api/tags   # → 정상 응답
```

실제 WSL2 IP는 `172.25.38.4` (26이 아니라 25). 한 글자 차이로 삽질. 교훈: IP 입력 전 반드시 `ip addr show eth0`로 확인.

### num_ctx 65536 설정 (Modelfile)

OpenClaw는 최소 64k 컨텍스트 필요. 일회성 `--num-ctx` 대신 Modelfile로 영구 등록:

```bash
# WSL2에서
cat > ~/Modelfile << 'EOF'
FROM qwen2.5:14b
PARAMETER num_ctx 65536
EOF
ollama create qwen2.5-ctx:14b -f ~/Modelfile
# → success
```

생성된 `qwen2.5-ctx:14b`를 OpenClaw default 모델로 지정.

### 연동 확인

```bash
openclaw tui --local --message "안녕 테스트야"
# 정상 한국어 응답 + ollama/qwen2.5-ctx:14b | tokens 9.3k/66k (14%)
```

VM3 OpenClaw ↔ WSL2 Ollama 연동 성공. num_ctx 65536도 정상 적용(66k 표시).

### Gateway LAN 바인딩

n8n에서 OpenClaw를 호출하려면 Gateway가 외부 접근 가능해야 함.

기본값은 loopback(127.0.0.1)이라 외부 접근 불가. `0.0.0.0` 직접 입력은 거부됨(allowed: auto, lan, loopback, custom, tailnet).

```bash
openclaw config set gateway.bind lan
openclaw gateway restart
openclaw gateway status
# → bind=lan (0.0.0.0), Listening: *:18789
```

### Gateway 토큰 확인

```bash
cat ~/.openclaw/openclaw.json | grep -A2 '"token"'
# config get은 __OPENCLAW_REDACTED__로 가려짐 → 파일 직접 확인
```

### agent CLI 세션 충돌 (미해결, 내일 이어서)

`openclaw agent --agent main --message "..."` 실행 시:

```
GatewayClientRequestError: EmbeddedAttemptSessionTakeoverError:
session file changed while embedded prompt lock was released
```

원인 추정: 앞서 tui로 열었던 세션 파일이 잠금 충돌. 내일 `~/.openclaw/agents/main/sessions/*.jsonl` 초기화 후 재시도 예정.

---

## 막혔던 부분 종합

| 문제 | 원인 | 해결 |
|---|---|---|
| openclaw 명령어 없음 | `.npm-global/bin` PATH 미등록 | .bashrc에 PATH 추가 |
| Ollama 연결 실패 | base URL IP 오타 (26 vs 25) | ip addr로 확인 후 재설정 |
| Prometheus scrape 안됨 | operator는 configmap 패치 무시 | additionalScrapeConfigs Secret + CR 패치 |
| Gateway 외부 접근 불가 | loopback 바인딩 | gateway.bind를 lan으로 변경 |
| gateway.bind 0.0.0.0 거부 | 허용값은 키워드만 | lan 키워드 사용 |
| agent 세션 충돌 | tui 세션 파일 잔존 | (내일) 세션 파일 초기화 |

---

## 새로 배운 개념

- 하네스 엔지니어링 3대 요소: 가드레일(차단) + 관측성(로깅) + 피드백루프(재시도/에스컬레이션)
- 지수 백오프(exponential backoff): 재시도 간격을 2^n초로 늘려 서버 부하 방지 (1→2→4초)
- prometheus-operator의 additionalScrapeConfigs: configmap이 아니라 Secret + Prometheus CR 패치 방식으로 추가 scrape 타겟 등록
- Modelfile: Ollama 모델 파라미터(num_ctx 등)를 영구 설정하는 방법
- openclaw gateway.bind: 0.0.0.0 직접 입력 불가, lan/loopback 등 키워드 사용

---

## 클로드 활용 사례

- OpenClaw 설치 마법사 단계별 선택 가이드 (provider/channel/skills/hooks)
- openclaw PATH 문제 진단 및 해결
- Ollama 연동 config 설정 + IP 오타 발견
- Modelfile로 num_ctx 영구 설정 방법
- Prometheus operator additionalScrapeConfigs 방식 디버깅
- guardrail/observability/feedback_loop 모듈 설계 및 연결 코드 작성
- Grafana PromQL 패널 쿼리 작성

---

## 내일 할 것

- agent CLI 세션 충돌 해결 (`~/.openclaw/agents/main/sessions/*.jsonl` 초기화)
- openclaw agent CLI 정상 동작 확인 (LLM 분석 응답)
- n8n 워크플로우에 OpenClaw 연동: Grafana → n8n → OpenClaw(가드레일 + Ollama) → Slack
- 최종 통합 시나리오 테스트 (minikube 부하 → 알림 → 자동 분석 → 슬랙)
- aiops-platform GitHub README 작성 + push
- Phase 5 최종 완성

---

## 오늘의 한 줄 회고

하네스 엔지니어링 3대 모듈(가드레일/관측성/피드백루프)을 직접 구현하고 Grafana로 시각화, OpenClaw 설치 및 Ollama 연동까지 하루에 진행. Prometheus operator의 additionalScrapeConfigs 방식과 IP 오타 한 글자 삽질이 가장 기억에 남는 교훈. agent CLI 세션 충돌만 해결하면 내일 최종 통합 파이프라인으로 Phase 5를 마무리할 수 있을 것 같다.
