TIL - 2026-05-21
오늘의 학습 개요
Phase 5(하네스 엔지니어링 + OpenClaw) 본격 진행. VM3에 OpenClaw 설치 및 WSL2 Ollama 연동 완료. 가드레일/관측성/피드백루프 3대 하네스 모듈 구현 및 Grafana 대시보드 시각화까지 완성.

Phase 5: 하네스 엔지니어링
5-1. guardrail.py 구현 및 테스트
동작 확인

ls /tmp → SUCCESS (허용 패턴 통과)
rm -rf /tmp/test → BLOCKED (차단 패턴 즉시 차단)
출력 민감정보 필터링 (password=, api_key= 등)

핵심 구조

BLOCKED_COMMANDS: 위험 명령어 정규식 목록
ALLOWED_PATTERNS: 허용 명령어 화이트리스트
check_output(): 출력값 민감정보 필터링

5-2. observability.py + metrics_exporter.py 구현
JSONL 로깅

AgentAction dataclass로 모든 에이전트 행동 기록
timestamp, agent_id, action_type, input, output, status, duration_ms 필드

Prometheus 메트릭 export

prometheus-client 라이브러리 사용
포트 8001로 메트릭 노출
agent_actions_total{status}: 액션 수 (success/blocked/failed)
agent_action_duration_ms: 실행 시간 히스토그램

5-3. feedback_loop.py 구현
3단계 동작 흐름

가드레일 체크 → 차단 시 즉시 반환
실행 → 성공 시 로깅 후 반환
실패 시 지수 백오프로 최대 3회 재시도 → 초과 시 에스컬레이션

테스트 결과

SUCCESS: 정상 명령어 실행
BLOCKED: 위험 명령어 즉시 차단
ESCALATION: 3회 재시도 후 사람에게 알림

5-4. Prometheus + Grafana 에이전트 행동 시각화
Prometheus 수집 연결 (삽질 포인트)

configmap 패치 방식 → 안 먹힘 (prometheus-operator 방식이라)
해결: additionalScrapeConfigs Secret으로 별도 생성 후 Prometheus CR에 패치

bashkubectl -n monitoring create secret generic additional-scrape-configs \
  --from-file=additional-scrape-configs.yaml=/tmp/additional-scrape.yaml

kubectl -n monitoring patch prometheus kube-prometheus-stack-prometheus \
  --type merge \
  -p '{"spec":{"additionalScrapeConfigs":{"name":"additional-scrape-configs","key":"additional-scrape-configs.yaml"}}}'
Grafana 대시보드 구성 (AIOps Agent Monitor)

액션 현황: sum(agent_actions_total) by (status)
차단 비율: rate(agent_actions_total{status="blocked"}[5m])
평균 실행 시간: rate(agent_action_duration_ms_sum[5m]) / rate(agent_action_duration_ms_count[5m])

5-5. VM3 OpenClaw 설치 + WSL2 Ollama 연동
설치 과정 핵심

Model/auth provider → Skip for now (Ollama config로 직접 설정)
Search provider → Skip for now
Skills/Hooks → Skip for now
Hatch → Hatch later (config 먼저 설정 후 실행)

PATH 문제

설치 후 openclaw: command not found
원인: ~/.npm-global/bin이 PATH에 없음
해결: echo 'export PATH="$HOME/.npm-global/bin:$PATH"' >> ~/.bashrc && source ~/.bashrc

Ollama 연동 설정
bashopenclaw config set models.providers.ollama.apiKey "ollama-local"
openclaw config set models.providers.ollama.baseUrl "http://172.25.38.4:11434"
openclaw config set agents.defaults.model.primary "ollama/qwen2.5-ctx:14b"
openclaw models auth login --provider ollama  # Local only 선택
openclaw gateway restart
IP 오타 주의

172.26.38.4 입력 → Ollama 연결 실패
실제 WSL2 IP: 172.25.38.4 (25 vs 26 차이)
교훈: IP 입력 시 반드시 ip addr show eth0로 먼저 확인

연동 확인
bashopenclaw tui --local --message "안녕 테스트야"
# ollama/qwen2.5-ctx:14b | tokens 9.3k/66k (14%) → 정상 응답
num_ctx 65536 설정 (Modelfile)
bashcat > ~/Modelfile << 'EOF'
FROM qwen2.5:14b
PARAMETER num_ctx 65536
EOF
ollama create qwen2.5-ctx:14b -f ~/Modelfile
Gateway LAN 바인딩

기본값: loopback (127.0.0.1) → 외부 접근 불가
해결: openclaw config set gateway.bind lan → Listening: *:18789


막혔던 부분 정리
문제원인해결openclaw 명령어 없음PATH 미등록.bashrc에 .npm-global/bin 추가Ollama 연결 실패IP 오타 (26 vs 25)ip addr로 확인 후 재설정Prometheus scrape 안됨prometheus-operator는 configmap 패치 무시additionalScrapeConfigs Secret 방식 사용Gateway 외부 접근 불가loopback 바인딩gateway.bind를 lan으로 변경agent 세션 충돌tui 세션 파일 잔존내일 세션 파일 초기화 후 재시도

새로 배운 개념

하네스 엔지니어링 3대 요소: 가드레일(차단) + 관측성(로깅) + 피드백루프(재시도/에스컬레이션)
지수 백오프: 재시도 간격을 2^n초로 늘려서 서버 부하 방지 (1초 → 2초 → 4초)
prometheus-operator additionalScrapeConfigs: configmap이 아닌 Secret + CR 패치 방식
openclaw gateway.bind lan: 0.0.0.0 직접 입력 불가, lan 키워드 사용


내일 할 것

세션 파일 충돌 해결 (~/.openclaw/agents/main/sessions/*.jsonl 초기화)
openclaw agent CLI 정상 동작 확인
n8n 워크플로우에 OpenClaw 연동 (Grafana → n8n → OpenClaw → Ollama → Slack)
aiops-platform GitHub README 작성
Phase 5 최종 완성 🎉


오늘의 한 줄 회고
하네스 엔지니어링 3대 모듈(가드레일/관측성/피드백루프) 구현부터 Grafana 시각화, OpenClaw 설치까지 하루에 완성. 특히 Prometheus scrape 연동에서 prometheus-operator 방식의 차이를 몸으로 배웠고, IP 오타 한 글자 차이로 삽질한 것도 좋은 교훈. 내일 최종 통합 파이프라인만 완성하면 Phase 5 끝.
