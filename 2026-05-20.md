TIL - 2026-05-20
오늘 배운 것
Phase 3: 쿠버네티스 + 모니터링
핵심 개념

Pod = 컨테이너를 묶은 쿠버 최소 실행 단위. 일회용(ephemeral)이라 죽으면 새로 생성됨. 해시값이 버전 구분자 역할.
Deployment = Pod 복제 관리 + 롤링 업데이트 + 자가 치유
Service = Pod IP는 휘발성이라 직접 접근 불가 → Service가 안정적 진입점 + 로드밸런싱 제공
HPA = CPU 사용률 보고 replicas 자동 조정 (min~max 범위 내)
네임스페이스 = 리소스 그룹화하는 폴더 개념 (default, monitoring 분리)

실습 완료

nginx Deployment 3개 Pod 배포 + NodePort Service 외부 노출
롤링 업데이트 (nginx 1.25 → 1.26): 해시값 58d88d5558 → 64ff848b9f 교체 관찰
롤백: rollout undo로 이전 버전 복구
HPA: CPU limit 100m으로 낮추고 부하 생성기 5개 동시 띄워서 Pod 자동 증가 확인
Prometheus + Grafana: kube-prometheus-stack Helm 차트로 일괄 설치
PromQL 직접 작성: mountpoint가 환경마다 달라서 raw 메트릭 먼저 조회 후 쿼리 맞춤
Grafana 알림 규칙 생성 (nginx Pod 재시작 3회 초과)
Grafana → 슬랙 Contact point 연동

깨달은 것: 왜 LLM이 필요한가

Grafana 알림 = "뭐가 잘못됐다"만 알려줌
원인 분석은 여전히 사람이 직접 해야 함
→ Phase 4에서 LLM으로 자동 분석 붙이는 이유


Phase 4: LLM + RAG + n8n 통합 파이프라인
핵심 개념

RAG = 외부 문서 검색 + LLM 답변 보강. 사내 특화 지식 활용 가능
Embedding = 텍스트를 숫자 벡터로 변환. 의미가 비슷한 텍스트는 벡터 공간에서 가까움
Vector DB = 벡터 저장 + 유사도 검색. 질문과 가장 관련 있는 청크 찾아줌
LCEL = LangChain Expression Language. 파이프(|) 연산자로 컴포넌트 연결하는 최신 방식
로컬 LLM (Ollama) = 토큰 비용 0원 + 폐쇄망 적용 가능 + 데이터 외부 유출 없음

실습 완료

Ollama 설치 + RTX 4060 GPU 가속 확인 (VRAM 7348MiB / 8188MiB 사용)
qwen2.5:14b 모델 다운로드 + 로컬 추론 테스트
LangChain RAG 챗봇 완성

문서: nms_guide.txt (SNMP, CPU, Elasticsearch, Kafka 트러블슈팅)
임베딩: paraphrase-multilingual-MiniLM-L12-v2
벡터DB: ChromaDB
가드레일 확인: "오늘 점심 추천해줘" → "문서에 없는 내용입니다"


VM3에 n8n Docker 컨테이너로 설치
통합 파이프라인 완성: Grafana → n8n Webhook → Ollama API → Slack
FastAPI로 RAG API 서빙

POST /ask: 질문 → RAG 답변
GET /docs: Swagger UI 자동 생성 (코드가 곧 문서)



RAM vs VRAM 차이

RAM = CPU가 쓰는 작업공간 (OS, 앱, 일반 프로세스)
VRAM = GPU가 쓰는 작업공간 (모델 가중치 저장 + 연산)
14B 모델이 VRAM에 딱 맞게 올라가야 GPU 풀가속 가능


막혔던 부분

langchain.text_splitter, langchain.chains, langchain.prompts 전부 ModuleNotFoundError

langchain 버전업으로 모듈 위치가 다 바뀜
langchain_text_splitters, langchain_core, LCEL 방식으로 전면 교체 필요


n8n JSON Body에 동적 표현식 넣으면 "Bad control character" 에러

Specify Body를 "Using Fields Below"로 변경 후 fx(Expression) 모드로 해결


n8n stream 파라미터 타입 에러

"false" 문자열이 아니라 ={{ false }} boolean으로 입력해야 함


Grafana PromQL mountpoint="/" 안 나옴

minikube 환경은 mountpoint가 /data, /var 등 다름
raw 메트릭 먼저 조회해서 실제 값 확인 후 쿼리 맞추는 습관 중요




해결 방법

langchain 에러: 에러 나는 임포트마다 최신 패키지로 교체 + pip install langchain-core langchain-text-splitters
n8n JSON 에러: "Using Fields Below" 모드 + fx Expression 모드 조합
PromQL 환경 차이: node_filesystem_avail_bytes raw 조회 → 실제 mountpoint 확인 → 쿼리 수정


클로드 활용

langchain 버전 에러 연속으로 터질 때 에러 메시지 그대로 붙여넣어서 최신 방식으로 전면 교체
n8n JSON 에러 디버깅 (동적 표현식 처리 방법)
PromQL mountpoint 환경 차이 해결
NotebookLM 레퍼런스 자료 (앤서블, 쿠버, LLM/RAG) 정리


내일 할 것

Phase 4 NotebookLM LLM/RAG 레퍼런스 구성 완료
Phase 5 시작: 하네스 엔지니어링 핵심 개념 (가드레일, 관측성, 피드백 루프)
guardrail.py 작성 (위험 명령어 차단 시스템)
VM3에 OpenClaw 설치 + WSL2 Ollama 연동
