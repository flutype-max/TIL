# TIL - 2026-05-20

## 오늘의 학습 개요

Phase 2 마무리(Role 리팩토링 + 포트폴리오 프로젝트)부터 Phase 3(쿠버네티스 + 모니터링) 완성, Phase 4(LLM + RAG + n8n 통합 파이프라인) 완성까지 진행. AIOps 로드맵의 핵심 인프라 구축을 하루에 완료한 압축 학습일.

---

## Phase 2 마무리: Ansible Role + 포트폴리오

### 2-3. Role 구조 리팩토링

**Role이란**
- 단일 플레이북 파일을 역할별 폴더 구조로 분리
- tasks/handlers/vars/templates로 관심사 분리
- 재사용성 + 가독성 + 협업 표준화

**기존 install_nginx.yml → Role 구조로 분리**

roles/nginx/
├── tasks/main.yml      # 할 일 목록
├── handlers/main.yml   # nginx 재시작 등
├── vars/main.yml       # nginx_port 등 변수
└── templates/          # nginx.conf.j2

- 플레이북은 `roles: [nginx]` 한 줄로 간소화

**막혔던 부분: Role을 못 찾는 에러**
- 증상: `the role 'nginx' was not found`
- 원인: site.yml이 playbooks/ 안에 있어서 roles 경로를 playbooks/roles에서 찾음
- 해결: 프로젝트 루트에 ansible.cfg 생성 + roles_path 명시

[defaults]
roles_path = ~/ansible-practice/roles
inventory   = ~/ansible-practice/inventory.ini

### 2-5. 포트폴리오 프로젝트: ansible-network-monitor

**프로젝트 구조 (별도 폴더로 분리)**
- ~/ansible-practice = 학습용 연습장
- ~/ansible-network-monitor = GitHub에 올릴 포트폴리오

**Role 3개 구성**
- common: SNMP 에이전트(snmpd) 설치
- monitoring: CPU/메모리 메트릭 수집
- alerting: 임계값 초과 시 슬랙 알림

**구성 요소**
- inventory/production.ini (vm1, vm2 타겟)
- playbooks/collect_metrics.yml (전체 실행)
- templates/report.html.j2 (HTML 리포트)
- 프로젝트 전용 ansible.cfg

**GitHub 업로드**
- 막혔던 부분: `git add .` 시 `does not have a commit checked out`
- 원인: playbooks/ 하위에 .git 폴더가 중첩됨
- 해결: `rm -rf playbooks/.git`
- README.md 작성 후 push 완료
- 결과: https://github.com/flutype-max/ansible-network-monitor

---

## Phase 3: 쿠버네티스 + 모니터링 스택

### 3-1. minikube 클러스터 구성

**환경 정보**
- WSL2 (Ubuntu 26.04)
- minikube v1.38.1 (Docker 드라이버)
- Kubernetes v1.35.1
- CPU 4코어, 메모리 8GB 할당

**시작 과정에서 막힌 점**
- 초기에 root 계정으로 minikube 실행 시도 → `DRV_AS_ROOT` 에러
- 원인: Docker 드라이버는 보안상 root 권한 사용 금지
- 해결: 일반 사용자(test) 계정 생성 → docker 그룹 추가 → 재실행
- 명령어: `sudo usermod -aG docker test`

**kubectl 다운로드 실패 경험**
- 증상: kubectl 실행 시 `syntax error near unexpected token '<'`
- 원인: curl 명령어를 두 줄로 입력하면서 URL이 깨져 XML 에러 페이지가 kubectl 파일로 저장됨
- 해결: 명령어를 반드시 한 줄로 입력 + 다운로드 후 파일 크기 확인(50MB 이상이어야 정상)
- 교훈: 다운로드 후 `ls -lh` 로 크기 검증하는 습관

### 3-2. nginx Deployment 배포

**핵심 개념 정리**

Pod
- 컨테이너 1개 이상을 묶은 쿠버 최소 실행 단위
- 일회용(ephemeral): 죽으면 새로 생성, 부활 X
- 고유 IP 가짐 (휘발성)
- 직접 만들지 않고 Deployment가 관리하는 게 일반적

Deployment
- Pod 복제 관리 (replicas)
- 롤링 업데이트 + 롤백
- 자가 치유 (Pod 죽으면 자동 재생성)

Service
- Pod IP는 휘발성이라 직접 접근 불가 → Service가 안정적 진입점 제공
- 라벨 셀렉터로 대상 Pod 결정
- NodePort 타입으로 외부 노출 (30000-32767 포트)

**실습 매니페스트 구조**
- apiVersion: apps/v1, kind: Deployment
- replicas: 3
- resources.requests/limits 설정 (CPU/메모리 보장량과 최대치)
- livenessProbe 적용 (HTTP 200 헬스체크)
- Service type: NodePort

**배포 후 확인**
- `kubectl get pods` → 3개 Pod Running 1/1
- `kubectl get service` → NodePort 32469 할당
- `curl http://192.168.49.2:32469` → Nginx Welcome 페이지 응답

### 3-3. 롤링 업데이트와 롤백

**롤링 업데이트 메커니즘 체감**

nginx 1.25 → 1.26 업데이트 시 관찰한 Pod 변화:
- 구버전 Pod 해시값: 58d88d5558
- 신버전 Pod 해시값: 64ff848b9f
- 두 해시값 Pod가 동시에 존재하다가 점진적으로 신버전으로 교체됨
- 항상 5개 이상 유지하면서 무중단 배포

명령어
- `kubectl set image deployment/nginx-deployment nginx=nginx:1.26`
- `kubectl rollout status deployment/nginx-deployment`
- `kubectl rollout undo deployment/nginx-deployment`
- `kubectl rollout history deployment/nginx-deployment`

**핵심 깨달음**: Pod 개수가 늘어나는 게 아니라 "교체"되는 것. 새 Pod Running 확인 후 구 Pod Terminating → 진짜 무중단 보장

### 3-4. HPA (Horizontal Pod Autoscaler)

**개념**
- CPU 사용률 보고 replicas 자동 조정 (min~max 범위 내)
- AWS 오토스케일링과 동일 개념
- NMS의 임계값 알림 + 수동 조치 → 자동 조치로 진화

**replicas vs HPA 관계**
- replicas: 매니페스트의 고정값
- HPA가 켜져 있으면 HPA가 replicas 값을 동적으로 덮어씀
- 매니페스트 replicas는 무시됨

**부하 테스트 시행착오**
- 처음에 부하 생성기 1개로 테스트 → CPU 1%/50%로 안 늘어남
- 원인: busybox wget 요청이 너무 가벼움 + Pod 5개에 분산되어 CPU 거의 0
- 해결: 부하 생성기 5개 동시 + CPU limit 100m으로 낮춰서 HPA 트리거되게 조정

**실험 명령어**
- `kubectl autoscale deployment nginx-deployment --min=2 --max=10 --cpu-percent=50`
- 부하 발생기: `kubectl run load-gen --image=busybox:1.28 ...`

### 3-5. Prometheus + Grafana 스택

**설치 방법**
- Helm 패키지 매니저 사용
- kube-prometheus-stack 차트 일괄 설치 (Prometheus + Grafana + AlertManager + node-exporter + kube-state-metrics 한 번에)
- monitoring 네임스페이스 분리

**Grafana 접속**
- `kubectl port-forward -n monitoring svc/kube-prometheus-stack-grafana 3000:80`
- 브라우저: http://localhost:3000 (admin / admin123)
- port-forward 원리: WSL2 localhost:3000 ↔ 클러스터 내부 Grafana 터널링 (SSH 포트포워딩과 동일)

**시간대 설정**
- Grafana 기본값이 UTC라 한국과 9시간 차이
- Profile → Preferences → Timezone → Browser Time으로 변경

**전통 NMS vs Grafana 비교**
- 전통 NMS = 완성품 PC (수집 + 저장 + 시각화 + 알림 올인원)
- Prometheus + Grafana = 조립 PC (역할별 도구 분리 = 모듈러 구조)
- Grafana는 쿠버 전용이 아닌 범용 시각화 도구 (MySQL, Elasticsearch, CloudWatch 등 데이터소스 자유)

### 3-6. PromQL 직접 작성

**기본 문법**
- `메트릭이름{라벨="값", 라벨2=~"정규식.*"}[시간범위]`
- 함수: rate(), sum(), avg(), max(), by(라벨) 그룹화

**실습 쿼리**
- nginx Pod CPU 사용량: `sum(rate(container_cpu_usage_seconds_total{namespace="default", pod=~"nginx-deployment.*"}[5m])) by (pod)`
- 노드 CPU 사용률: `100 - (avg(rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)`
- 디스크 사용률: `node_filesystem_avail_bytes` 기반 계산

**환경 차이 디버깅**
- 공식 문서의 `mountpoint="/"` 쿼리가 minikube에서 결과 없음
- 원인: minikube는 mountpoint가 `/data`, `/var`, `/etc/hostname` 등 다름
- 해결 순서: raw 메트릭 `node_filesystem_avail_bytes` 먼저 조회 → 실제 mountpoint 값 확인 → 쿼리에 정확한 값 입력
- 대안: `fstype="ext4"` 조건으로 가상 파일시스템 제외하고 실제 디스크만 필터링
- 교훈: 실무에서도 환경마다 라벨값이 다르니 raw 메트릭 먼저 확인하는 습관 필수

### 3-7. Grafana 알림 + 슬랙 연동

**알림 규칙 생성**
- Folder: nginx-alerts
- PromQL: `kube_pod_container_status_restarts_total{namespace="default"}`
- Threshold: IS ABOVE 3
- Evaluation: 1분마다

**알림 상태 종류**
- 🟢 Normal (조건 미충족)
- 🟠 Pending (조건 충족, Pending period 대기 중)
- 🔴 Firing (발동! 알림 전송)

**중요한 깨달음**
- `kubectl delete pod`는 RESTARTS 카운트에 포함되지 않음 (Pod 자체 삭제 후 새 Pod 생성)
- 진짜 재시작 카운트는 OOMKilled, CrashLoopBackOff, livenessProbe 실패 등일 때만 올라감
- 학습 목적이면 PromQL을 `vector(5)`로 바꿔서 임시로 Firing 트리거 가능

**슬랙 Contact point 연동**
- Slack Incoming Webhook URL 생성
- Grafana Alerting → Contact points → Webhook 추가
- Test 버튼으로 메시지 도착 확인

### 3-8. "왜 LLM이 필요한가" 체감 단계

**OOM 실습으로 단순 알림의 한계 경험**
- 메모리 limit을 10Mi로 일부러 낮춤
- 부하 주면 Pod가 OOMKilled로 죽음 → CrashLoopBackOff
- 디버깅 골든 패스 직접 경험:
  1. `kubectl get pods` → STATUS 확인
  2. `kubectl describe pod` → Events 섹션 보기
  3. `kubectl logs --previous` → 직전 컨테이너 로그
  4. `kubectl top pod` → 리소스 사용량
  5. Grafana 대시보드 → 시계열 메트릭

**핵심 깨달음**
- Grafana 알림은 "뭐가 잘못됐다"만 알려줌
- 원인 분석은 여전히 사람이 직접 해야 함
- "왜 그런지, 어떻게 해결할지"까지 자동화하려면 LLM 필요
- → Phase 4에서 LLM 붙이는 이유

---

## Phase 4: LLM + RAG + n8n 통합 파이프라인

### 4-1. Ollama 설치 + GPU 가속

**설치 과정**
- `sudo apt-get install -y zstd` (의존성 먼저 설치 필요)
- `curl -fsSL https://ollama.ai/install.sh | sh`
- 설치 시 자동으로 systemd 서비스 등록 + 시작됨
- 따라서 `ollama serve` 별도 실행 불필요 (이미 떠있음)

**GPU 가속 확인 (RTX 4060)**
- nvidia-smi로 모니터링
- 모델 다운로드 중에는 GPU 메모리 변화 없음 (디스크 작업)
- 모델 실행(ollama run) 시점에 VRAM 사용량 급증
- 최종: VRAM 7348MiB / 8188MiB (모델이 VRAM에 거의 다 올라감)
- GPU-Util 25% (추론 중)

**RAM vs VRAM 차이 정리**
- RAM = CPU가 쓰는 작업공간 (OS, 앱, 일반 프로세스)
- VRAM = GPU가 쓰는 작업공간 (모델 가중치 저장 + 연산)
- 비유: CPU=직원, RAM=직원 책상 / GPU=공장기계, VRAM=공장 작업대
- 14B 모델이 VRAM에 딱 맞게 들어가야 GPU 풀가속 → 부족하면 CPU offload로 10~20배 느려짐

**외부 노출 설정 (VM3에서 접근하기 위함)**
- `sudo systemctl edit ollama`
- `Environment="OLLAMA_HOST=0.0.0.0:11434"` 추가
- systemctl restart ollama
- WSL2 IP 확인: `ip addr show eth0` → 172.25.38.4

### 4-2. LangChain RAG 챗봇

**RAG 핵심 개념**
- LLM에 외부 문서 검색 결과를 컨텍스트로 주입
- 학습 데이터 외의 정보(사내 문서, 최신 정보) 활용 가능
- 할루시네이션 감소 + 출처 명시 가능

**RAG 동작 원리**

인덱싱 단계 (한 번만):
- 문서 로드 → 청킹 → 임베딩 → 벡터DB 저장

질의 단계 (매번):
- 질문 → 임베딩 → 유사 청크 검색 → LLM 전달 → 답변

**구성 요소**
- 문서: nms_guide.txt (SNMP, CPU, Elasticsearch, Kafka 트러블슈팅 5가지)
- 임베딩 모델: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 (한국어 포함 다국어)
- 벡터DB: ChromaDB (로컬 파일 저장)
- LLM: Ollama qwen2.5:14b
- 청킹: chunk_size=500, chunk_overlap=50

**langchain 버전 문제 대거 발생**
- `from langchain.text_splitter` → ModuleNotFoundError
- `from langchain.chains` → ModuleNotFoundError
- `from langchain.prompts` → ModuleNotFoundError
- 원인: langchain이 여러 패키지로 분리됨 (langchain_core, langchain_community, langchain_text_splitters 등)
- 해결: 임포트 전부 최신 방식으로 교체 + RetrievalQA → LCEL 방식으로 전면 재작성

**LCEL (LangChain Expression Language) 패턴 학습**

```python
chain = (
    {"context": retriever | format_docs, "question": RunnablePassthrough()}
    | prompt
    | llm
    | StrOutputParser()
)
```

- 파이프(|) 연산자로 컴포넌트 연결
- 각 단계: 검색 → 포매팅 → 프롬프트 → LLM → 출력 파싱
- stream() 메서드로 실시간 타이핑 효과 가능

**가드레일 동작 확인**
- 문서 내 질문 ("CPU 95% 어떻게 해야 해?") → 정확한 답변
- 문서 외 질문 ("오늘 점심 추천해줘") → "문서에 없는 내용입니다"
- 프롬프트 엔지니어링으로 RAG 범위 제한 가능

**경고 메시지들 무시 가능**
- HuggingFaceEmbeddings deprecated → 동작은 정상
- CUDA driver too old → 임베딩만 CPU로 돌아감, Ollama LLM은 별개 프로세스라 GPU 정상 사용
- HF_TOKEN 없음 → 익명 다운로드, 무시 가능

### 4-3. n8n 워크플로우 자동화 도구

**VM3에 Docker 컨테이너로 설치**
- `curl -fsSL https://get.docker.com | sh`
- test 계정 docker 그룹 추가 후 재로그인
- n8n 컨테이너 실행

**초기 에러들**

권한 문제 (Restarting 반복)
- 증상: `EACCES: permission denied, open '/home/node/.n8n/config'`
- 원인: ~/.n8n 폴더가 root 소유라 컨테이너 내부 node 유저(uid=1000)가 못 씀
- 해결: `sudo chown -R 1000:1000 ~/.n8n`

Secure Cookie 에러
- 증상: HTTP 접속 시 보안 쿠키 거부
- 해결: 환경변수 추가 `-e N8N_SECURE_COOKIE=false`

**최종 실행 명령어**

```bash
docker run -d \
  --name n8n \
  --restart unless-stopped \
  -p 5678:5678 \
  -v ~/.n8n:/home/node/.n8n \
  -e N8N_HOST=192.168.56.103 \
  -e N8N_SECURE_COOKIE=false \
  n8nio/n8n
```

**계정 생성**
- 비밀번호 정책: 대문자 포함 필수 (admin1234! → Admin1234!)
- ~/.n8n 폴더에 데이터 영구 저장됨

### 4-4. Grafana → n8n → Ollama → Slack 통합 파이프라인

**전체 흐름**
- Grafana 알림 발생
- n8n Webhook으로 수신
- HTTP Request 노드가 Ollama API 호출 (분석)
- HTTP Request 노드가 Slack Webhook으로 결과 전송

**구축 단계**

Step 1: Ollama 외부 노출 + VM3에서 접근 확인
- VM3에서 `curl http://172.25.38.4:11434/api/tags` 응답 확인

Step 2: Grafana Contact point 추가
- Webhook 통합
- URL: `http://192.168.56.103:5678/webhook-test/grafana-alert`

Step 3: n8n Webhook 노드
- POST, Path: grafana-alert
- Listen for test event로 대기

Step 4: HTTP Request 노드 (Ollama 호출)
- Method: POST
- URL: `http://172.25.38.4:11434/api/chat`
- Body: model, messages, stream JSON

Step 5: HTTP Request1 노드 (Slack 전송)
- Slack Webhook URL
- Body에 LLM 응답 내용 포함

**JSON Body 트러블슈팅 대모음**

"Bad control character" 에러
- 원인: 동적 표현식(`$json.body.message`)에 줄바꿈(\n) 포함되어 JSON 깨짐
- 해결: Specify Body를 "Using JSON"에서 "Using Fields Below"로 변경 → 각 파라미터 개별 추가 → 값에 fx(Expression) 모드 사용

stream 타입 에러
- 증상: `cannot unmarshal string into Go struct field ChatRequest.stream of type bool`
- 원인: `"false"` 문자열로 인식됨
- 해결: fx 모드에서 `={{ false }}` boolean으로 입력

no_text 에러 (Slack)
- 증상: Slack이 빈 text 거부
- 원인: 단독 노드 테스트 시 이전 단계 데이터 없음
- 해결: 전체 워크플로우 Execute workflow + Grafana Test 트리거

text 파라미터 이름 오타
- 증상: 메시지가 슬랙에 안 옴
- 원인: 파라미터 이름을 `next`로 입력 (text여야 함)
- 해결: 파라미터 이름 수정

**노드 이름 참조 주의**
- 두 번째 HTTP Request 노드 이름이 자동으로 "HTTP Request1"이 됨
- Expression에서 참조 시 `$('HTTP Request').item.json.message.content` (첫 번째 노드 참조)
- 노드 이름 정확히 확인 필수

**슬랙 알림 메시지 nginx 중복 표시 문제**
- 증상: Grafana 원본 알림 + LLM 분석 메시지에서 nginx 관련 내용이 중복으로 표시됨
- 수정 완료: 메시지 텍스트 구성 정리하여 중복 제거

### 4-5. FastAPI로 RAG API 서빙

**왜 FastAPI인가**
- Python 기반 최고 성능 웹 프레임워크
- 타입 힌트만 작성하면 OpenAPI 스펙 자동 생성
- Swagger UI 자동 생성 (/docs 엔드포인트)
- 코드가 곧 문서가 되는 구조

**구현한 엔드포인트**
- GET `/` → 루트 메시지
- GET `/health` → 헬스체크
- POST `/ask` → 질문 받아 RAG 답변 반환
- GET `/docs-count` → 로드된 문서/청크 개수
- GET `/docs` → Swagger UI (자동)
- GET `/openapi.json` → OpenAPI 스펙 (자동)

**Pydantic 모델로 요청/응답 정의**
- QuestionRequest: question 필드
- AnswerResponse: question, answer, status 필드
- 타입 힌트 기반으로 자동 검증 + 문서화

**실행**
- `uvicorn rag_api:app --host 0.0.0.0 --port 8000 --reload`
- 0.0.0.0 바인딩으로 외부 접속 허용
- --reload로 코드 변경 시 자동 재시작

**테스트 결과**
- curl 테스트: NMS 트러블슈팅 질문에 정확한 답변
- Swagger UI에서 직접 테스트 가능
- 응답 시간: 로컬 GPU 기준 적당히 빠름

**Phase 4 통합 가치**
- RAG API를 n8n에서 호출 가능
- 미래 확장: Grafana 알림 → n8n → RAG API(/ask) → Slack
- 즉, "단순 알림"이 아니라 "사내 문서 기반 분석된 알림" 가능

---

## 막혔던 부분 종합

### Ansible Role 경로 못 찾음
- 증상: the role 'nginx' was not found
- 해결: ansible.cfg에 roles_path 명시

### git 중첩 .git 폴더
- 증상: does not have a commit checked out
- 해결: rm -rf playbooks/.git

### kubectl 다운로드 실패
- 증상: XML 에러가 kubectl 파일로 저장됨
- 원인: curl URL이 두 줄로 잘림
- 해결: 한 줄로 입력 + 파일 크기 검증

### minikube root 권한 에러
- 증상: DRV_AS_ROOT
- 해결: 일반 사용자(test) + docker 그룹

### langchain 모듈 위치 변경 (연속 발생)
- text_splitter, chains, prompts 전부 ModuleNotFoundError
- 해결: 임포트 전부 최신 방식으로 교체 + LCEL 패턴으로 전면 재작성

### Ollama 포트 충돌
- 증상: address already in use
- 원인: 설치 시 자동 시작됨
- 해결: 별도 ollama serve 불필요

### n8n 권한 문제
- 증상: ~/.n8n 접근 권한 거부로 Restarting 반복
- 해결: uid 1000으로 chown

### n8n Secure Cookie 거부
- 해결: N8N_SECURE_COOKIE=false 환경변수

### n8n JSON Body 동적 표현식 깨짐
- 원인: 줄바꿈 포함 시 JSON 파싱 실패
- 해결: Using Fields Below + fx Expression 모드

### 슬랙 알림 nginx 중복 표시
- 메시지 텍스트 구성 정리하여 중복 제거 완료

### PromQL mountpoint 환경 차이
- 원인: minikube 환경 mountpoint가 표준과 다름
- 해결: raw 메트릭 먼저 조회해서 실제 라벨값 확인

### git 인증 + 사용자 설정
- HTTPS push 시 Personal Access Token 필요
- empty ident name → git config user.name/email 설정
- credential.helper store로 토큰 저장

---

## 해결 방법 핵심 패턴

### 환경 차이 디버깅 패턴
공식 문서 쿼리/명령이 실패할 때:
1. raw 데이터 먼저 조회 (메트릭, 파일, 경로)
2. 실제 환경의 값 확인
3. 그 값에 맞게 쿼리/명령 수정

### langchain 같은 빠르게 변하는 라이브러리
- 에러 메시지 그대로 검색
- deprecated 경고 무시하지 말고 즉시 최신 방식으로 교체
- LCEL 패턴이 표준이라 기억해둘 것

### n8n 같은 노코드 도구의 JSON 처리
- 동적 표현식 쓸 땐 항상 Using Fields Below 모드
- boolean, 숫자는 fx Expression 모드 (따옴표 없이)
- 노드 이름 정확히 확인 후 참조

---

## 클로드 활용 사례

- Ansible Role 리팩토링 가이드 + ansible.cfg 경로 문제 해결
- git 중첩 .git 폴더 문제 진단
- langchain 버전 에러 연속 발생 시 임포트 전체 최신화 (한 번에 해결)
- LCEL 방식 RAG 체인 재작성
- n8n JSON 에러 디버깅 (동적 표현식 처리법)
- 슬랙 알림 nginx 중복 표시 수정
- PromQL mountpoint 환경 차이 해결
- minikube + Helm + Prometheus + Grafana 설치 가이드
- OOM 실습 시나리오로 LLM 필요성 체감
- FastAPI 코드 작성
- NotebookLM 레퍼런스 자료 정리 (앤서블, 쿠버, LLM/RAG 3종)
- TIL 마크다운 정리

---

## NMS 경력 활용 포인트

| NMS 경험 | 모던 스택 매핑 |
|---------|---------------|
| SNMP 폴링 | Prometheus scrape |
| 임계값 알림 | PromQL alert rules |
| 대시보드 | Grafana |
| 장비 모니터링 | Pod/Node 모니터링 |
| 로그 분석 | kubectl logs / Loki |
| 트러블슈팅 매뉴얼 | RAG 챗봇 소스 문서 |

이직 시 강점: 전통 NMS 운영 경험 + 모던 스택 학습 = 양손잡이

---

## 오늘 완성한 포트폴리오

- ansible-network-monitor (GitHub) - Phase 2 결과물, Role 구조 3개 (common/monitoring/alerting)
- RAG 챗봇 + FastAPI API (rag_chatbot.py, rag_api.py)
- Grafana → n8n → Ollama → Slack 통합 파이프라인

---

## 내일 할 것

- Phase 4 NotebookLM LLM/RAG 레퍼런스 구성 완료
- Phase 5 시작: 하네스 엔지니어링 핵심 개념 학습 (가드레일, 관측성, 피드백 루프)
- guardrail.py 작성 (위험 명령어 차단 시스템 직접 구현)
- VM3에 OpenClaw 설치
- OpenClaw → WSL2 Ollama API 연동
- 최종 통합 시나리오: Grafana → n8n → OpenClaw(가드레일 + Ollama) → 자동 진단/복구

---

## 오늘의 한 줄 회고

Phase 2 마무리부터 Phase 3, 4를 하루에 끝내는 페이스. NMS 엔지니어 경험이 쿠버네티스 모니터링 + Prometheus + Grafana에 자연스럽게 매핑되어 학습 속도가 빨랐다. Phase 4의 LLM/RAG는 새로운 영역이라 langchain 버전 문제, n8n JSON 처리 등 시행착오가 많았지만, 결국 Grafana 알림이 LLM 분석을 거쳐 Slack까지 전달되는 진짜 AIOps 파이프라인을 완성한 것이 가장 큰 성과. 폐쇄망 보안 회사에 어필 가능한 로컬 LLM 기반 솔루션의 윤곽이 잡혔다.
