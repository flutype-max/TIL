TIL - 2026-05-22
오늘의 학습 개요
Phase 5 최종 단계. OpenClaw agent CLI 정상화부터 시작해서, Grafana → n8n → OpenClaw(가드레일+Ollama) → Slack 통합 파이프라인을 end-to-end로 완성. 로컬 LLM 운영(VRAM 관리), n8n 2.x의 셸 노드 제거 대응, OpenClaw REST API 부재 우회(FastAPI 래퍼), 그리고 7b 모델의 한계를 프롬프트 설계로 보완하는 과정까지. 삽질이 가장 많았지만 실무 패턴이 많이 나온 날.

1. OpenClaw agent CLI 정상화
1-1. gateway timeout → embedded fallback 문제
첫 openclaw agent 실행이 9분 36초나 걸림. 로그 분석:

EMBEDDED FALLBACK: Gateway agent timed out / gateway timeout after 630000ms
gateway(ws://127.0.0.1:18789)가 630초간 무응답 → 타임아웃 → embedded 모드로 fallback
지정한 --session-id도 무시되고 gateway-fallback-... 세션으로 교체됨

1-2. 진짜 원인: 14b 모델 VRAM 초과 (CPU offload)
ollama ps로 진단:
qwen2.5-ctx:14b    18 GB    66%/34% CPU/GPU    32768

14b 가중치(~8GB) + KV 캐시(~4~6GB)가 VRAM 8GB(RTX 4060) 초과
초과분 66%가 CPU RAM으로 offload → GPU 대비 10~20배 느려짐
VM3 메모리(4GB) 문제 아님: 추론은 전부 WSL2 Ollama에서 돌고, VM3는 오케스트레이션만 함

핵심 개념: VRAM 사용처는 ① 모델 가중치 + ② KV 캐시 두 군데. 둘의 합이 VRAM 넘으면 CPU로 샌다. RTX 4060 8GB에서 GPU 풀가속 가능한 현실적 한계는 7b급.
1-3. 7b로 교체
bashollama stop qwen2.5-ctx:14b
cat > ~/Modelfile-7b << 'EOF'
FROM qwen2.5:7b
PARAMETER num_ctx 65536
EOF
ollama create qwen2.5-ctx:7b -f ~/Modelfile-7b
VM3에서:
bashopenclaw config set agents.defaults.model.primary "ollama/qwen2.5-ctx:7b"
openclaw gateway restart
결과: 실행 시간 9분36초 → 84초로 단축. fallbackUsed: false, gateway 정상 경유.

ollama create는 모델 "등록"만 함 → ollama ps에 안 뜨는 게 정상 (run/API 호출 시 로드)
14b가 메모리에 캐시로 남아있으면 ollama stop으로 강제 하차

1-4. sudo 권한 + 세션 오염

VM3에 NOPASSWD 미설정 → sudo top failed (VM1/VM2엔 했는데 VM3 누락)

해결: echo "test ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/test


같은 session-id 재사용 시 이전 실행 컨텍스트가 남아 엉뚱한 동작

해결: 세션 파일 삭제 또는 새 session-id 사용



최종적으로 toolSummary: { calls: 3, tools: ["memory_search","exec"], failures: 0 } 확인 → exec 정상 실행 + 가드레일 통과 확인.
1-5. qwen2.5의 중국어 출력 버릇

qwen2.5(Alibaba 모델)는 언어 미지정 시 중국어로 응답 튀는 경우 있음
해결: 메시지에 "한국어로 답변" 명시 (프롬프트가 가장 가벼운 해결책)


2. n8n ↔ OpenClaw 연동 (가장 많이 막힌 부분)
2-1. 근본 제약 파악
항목상태OpenClaw REST API❌ 없음 (/api/v1/... → 404) → CLI로만 호출n8n Execute Command 노드❌ 없음 (n8n 2.x에서 보안상 제거, 현재 2.21.5)n8n SSH 노드✅ 있음
간극: n8n 2.x는 컨테이너 내부 셸 직접 실행 불가 + OpenClaw는 REST 없음 → 이 둘을 어떻게 잇느냐가 관건.
2-2. 가능한 방법 4가지 검토

SSH 노드 직접 (빠르나 타임아웃 취약)
FastAPI 래퍼 REST API (가장 안정적) ← 채택
호스트에 n8n 직접 설치 (단독으론 해결 안 됨)
비동기 폴링 (느린 모델 대응, 복잡)

채택 이유: Phase 4에서 FastAPI 경험 있음(학습 연속성) + n8n은 HTTP Request 노드만 쓰면 됨 + 나중에 Grafana가 직접 호출하는 형태로도 재사용 가능.
2-3. SSH 시도 과정에서 배운 것

n8n은 Docker 컨테이너 → 127.0.0.1은 컨테이너 자신 → 호스트 접근 안 됨
docker inspect n8n | grep Gateway → 172.17.0.1(Docker 브릿지 게이트웨이)로 호스트 접근
n8n SSH credential "No testing function found" 는 에러 아님 (테스트 기능만 없음, 저장은 됨)
SSH로 OpenClaw는 실행됐으나 n8n SSH 노드 타임아웃을 UI에서 못 늘려서 한계

2-4. FastAPI 래퍼 구축
~/openclaw-api/main.py 핵심:
pythonenv = os.environ.copy()
env["PATH"] = "/home/test/.npm-global/bin:" + env.get("PATH", "")
env["HOME"] = "/home/test"
result = subprocess.run(cmd, capture_output=True, text=True, timeout=300, env=env)

python3-venv, python3-pip 먼저 설치 필요했음
POST /analyze 가 message 받아 openclaw 실행 후 결과 텍스트만 반환

n8n HTTP Request 노드: POST http://172.17.0.1:8800/analyze, body에 message 전달.

3. 메시지 전달 디버깅 (추측 함정에 빠졌던 구간)
증상
FastAPI 로그에 받은 메시지: = → message가 빈 문자열. OpenClaw가 NO_REPLY 즉시 반환(2.8초).
헛다리
표현식 경로($json.body... vs $json.body.body...)만 계속 바꿔봤지만 둘 다 [undefined]. → 경로 문제가 아니라 참조할 데이터 자체가 없는 상태였음.
진단 방법 (교훈)
n8n 표현식 미리보기가 [Execute previous nodes for preview] → 에디터에 직전 노드 실제 출력이 없으면 표현식 평가 자체가 안 됨.
해결 순서:

Webhook 노드 "Listen for test event"로 대기
test URL(/webhook-test/...)로 curl → 실제 데이터 캡처/고정
그제서야 HTTP Request 노드 미리보기 작동

진짜 원인
curl에 {"body":{"alerts":...}} 로 바깥 body 래퍼를 내가 임의로 씌웠던 것. 실제 Grafana는 {"alerts":[...]} 로 보냄. n8n이 받으면 $json.body 안에 담으므로 올바른 경로는:
={{ $json.body.alerts[0].annotations.description }}
(body 한 번. 내가 래퍼를 씌워서 body.body 두 번이 됐던 것)
미리보기에 "서버 CPU 사용률 95% 초과 감지" 뜨면서 경로 확정.
교훈: production webhook은 실행 결과가 안 보여서 깜깜이 추측을 부른다. test 모드로 데이터 흘려서 눈으로 보고 잡아야 한다.

4. 7b 모델 한계 → 프롬프트 설계로 보완
증상
파이프라인은 끝까지 도는데, OpenClaw가 top 대신 {"name":"read","arguments":{...}} 같은 툴 호출 JSON을 텍스트로 출력. 과거 세션(salty-lagoon, /tmp/top_output.txt)도 섞임.
원인: 7b는 작은 모델이라 "툴 호출해야지" 판단은 하나 실제 툴 호출 형식으로 못 내보내고 텍스트로 뱉음 + memory_search가 과거 세션 끌어옴.
방향 결정
지금 OpenClaw 제어 대상이 VM3뿐이라 실제 진단 가치 적음 → "분석가"로 운영(명령 실행 X, 알람 텍스트만으로 원인/조치 설명)이 더 안정적이고 깔끔. 실제 진단 에이전트 실험은 나중에 PC에 OpenClaw 올려서 별도 미니 프로젝트로.
해결: AGENTS.md 최상단에 역할 블록 주입
bashcat role-header.md ~/.openclaw/workspace/AGENTS.md > new.md && mv new.md AGENTS.md
역할 지시: "모니터링 알람 분석가, 명령 실행 금지, 툴 호출 JSON 출력 금지, 과거 세션 의존 금지, 한국어로 원인후보→권장조치, 5줄 이내"
결과: top 실행 시도 X, 툴 JSON X, 세션 오염 X → "원인 후보 → 권장 조치" 깔끔한 한국어 분석 출력. 최종 통합 파이프라인 end-to-end 동작 확인 완료.

막혔던 부분 종합
문제원인해결OpenClaw 9분 소요14b VRAM 초과 CPU offload 66%7b로 교체gateway timeout fallback위와 동일(추론 지연)7b 교체로 해소sudo top failedVM3 NOPASSWD 미설정sudoers.d/test 설정세션 오염session-id 재사용새 id / 세션파일 삭제중국어 출력qwen2.5 기본 버릇프롬프트에 한국어 명시Execute Command 없음n8n 2.x 제거SSH→최종 FastAPI 래퍼컨테이너 SSH 불가127.0.0.1=컨테이너 자신172.17.0.1(브릿지 게이트웨이)message 빈값curl에 body 래퍼 임의 추가$json.body.alerts...(body 1번)subprocess 동작 이상PATH/HOME 환경변수 누락env 복사 후 PATH/HOME 주입툴호출 JSON 텍스트 출력7b 모델 한계AGENTS.md 역할 지정(분석가)

해결 방법 핵심 패턴
로컬 LLM VRAM 진단

ollama ps의 PROCESSOR 컬럼이 xx%/yy% CPU/GPU면 offload 발생 = 느림의 원인
VRAM = 가중치 + KV 캐시. 합이 VRAM 넘으면 샌다. num_ctx 키우면 KV 캐시도 커짐
8GB GPU의 현실적 한계는 7b급

노코드 도구 우회 설계

도구가 막으면(셸 노드 제거) 가장 잘 다루는 인터페이스(HTTP)로 우회
CLI만 있는 대상은 얇은 REST 래퍼(FastAPI)로 감싸면 통합이 쉬워짐

데이터 흐름 디버깅

"표현식이 틀렸나" 의심 전에 "참조 데이터가 있나"부터 확인
production(결과 안 보임) 말고 test 모드로 실제 데이터 흘려서 눈으로 확인
내가 임의로 만든 페이로드 구조가 실제와 다를 수 있음을 항상 의심

subprocess로 외부 CLI 호출

셸과 달리 PATH, HOME 등 환경변수가 기본으로 안 잡힘 → env에 명시적으로 주입


클로드 활용 사례

ollama ps 출력으로 VRAM 초과/CPU offload 진단 및 7b 교체 처방
OpenClaw gateway timeout 로그 분석(fallback 메커니즘 해석)
n8n ↔ OpenClaw 연동 4가지 방법 비교 후 FastAPI 래퍼 채택
FastAPI subprocess 환경변수(PATH/HOME) 문제 진단
message 빈값 디버깅 — 추측 멈추고 test 모드로 데이터 고정하는 방법 제시
7b 툴호출 한계를 AGENTS.md 역할 지정으로 우회하는 설계 판단
TIL 작성


새로 배운 개념

VRAM 구성: 모델 가중치 + KV 캐시. CPU offload 발생 조건과 진단법
n8n 2.x 보안 정책: Execute Command 노드 제거, 컨테이너 셸 실행 제약
Docker 브릿지 게이트웨이: 컨테이너에서 호스트 접근은 172.17.0.1
얇은 REST 래퍼 패턴: CLI 전용 도구를 FastAPI로 감싸 통합성 확보
subprocess 환경변수: 셸 세션과 달리 PATH/HOME 미상속 → 명시 주입 필요
n8n 표현식 평가 조건: 직전 노드 실제 출력이 있어야 평가/미리보기 가능
작은 모델(7b)의 tool calling 한계: 판단은 하나 형식 출력 실패 → 프롬프트로 역할 제한이 현실적 보완책


오늘의 한 줄 회고
Grafana → n8n → OpenClaw(가드레일+Ollama) → Slack 통합 파이프라인 end-to-end 완성. Phase 5가 닫혔고 사실상 로드맵 한 바퀴 완주. 삽질이 가장 많았지만(VRAM, 셸 노드 제거, body 래퍼, subprocess 환경변수, 7b 한계) 전부 실무에서 그대로 나오는 패턴이라 가장 많이 배운 날. 특히 "표현식 추측을 멈추고 데이터를 직접 봐라"는 디버깅 교훈이 컸다. 로컬 LLM은 결국 하드웨어(VRAM)가 모델 크기를 결정한다는 것도 체감.

내일 할 것

(로드맵 완주 마무리) NotebookLM 하네스 엔지니어링 노트에 Phase 5 자료 누적
깊이 다지기 1차: 클로드 도움 없이 핵심 조각 백지 재현 (guardrail.py 정규식, RAG LCEL 체인 등)
(별도 미니 프로젝트) PC(WSL2 호스트)에 OpenClaw 올려서 실제 minikube 클러스터 진단 에이전트로 실험 — AGENTS.md 역할 블록 + exec 허용으로 전환
