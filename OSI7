# OSI 7계층 모델 정리 (Q&A 기반)

> 네트워크/인프라 엔지니어 관점에서 직접 질문하며 이해한 내용을 정리한 TIL

---

## 1. 계층별 핵심 정리

| 계층 | 이름 | 핵심 이해 | 정답 / 보충 설명 |
|------|------|-----------|------------------|
| **1** | 물리 (Physical) | 랜선 꽂아서 연결된 딱 그까지 | ✅ 물리적 연결 자체. NIC이 0/1을 전기·광·전파 신호로 변환. 링크 UP/DOWN이 1계층 체크 |
| **2** | 데이터링크 (Data Link) | VLAN 분리 + L2스위치 MAC 저장 | ✅ MAC 주소로 어디 보낼지 결정 + VLAN으로 논리적 분리. IP 몰라도 동작. (ARP는 2~3계층 경계, 보통 2계층 분류) |
| **3** | 네트워크 (Network) | IP로 핑 날리고 OSPF로 라우팅 | ✅ 목적지 IP 보고 경로 결정. ping = ICMP라 3계층. 라우터 / L3 스위치 |
| **4** | 전송 (Transport) | 포트로 구분, TCP는 핸드쉐이크로 연결 | ✅ 포트로 프로세스 식별. TCP = 3-way handshake 신뢰성, UDP = 빠른 비신뢰 |
| **5** | 세션 (Session) | 세션이면 그냥 TCP 아니야? | △ 현실에선 TCP와 구분 잘 안 함. 추가로 체크포인트(재개), 다중 연결 묶음 관리. TCP/IP 모델은 7·6·5를 응용으로 통합 |
| **6** | 표현 (Presentation) | SSL 인증서? UTF-8 / EUC-KR 인코딩 | ✅ TCP 위에서 암호화 협상(SSL handshake) + 바이트를 어떤 문자로 해석할지(인코딩). 인코딩 안 맞으면 글자 깨짐 |
| **7** | 응용 (Application) | HTTP / SSH / SNMP / Syslog 등 프로토콜 기능 자체 | ✅ TCP/UDP 골라 쓰며 "데이터로 뭘 할지" 정의. SNMP/Syslog = UDP, HTTP/SSH = TCP |

---

## 2. 큰 그림: 두 영역으로 나누기

| 구분 | 계층 | 역할 한 줄 요약 |
|------|------|-----------------|
| **인프라 영역** | 1 ~ 4 | "데이터를 **어떻게 전달**할까" — 네트워크/인프라 엔지니어 주 업무 |
| **애플리케이션 영역** | 5 ~ 7 | "전달된 데이터로 **뭘 할까**" — TCP 세션을 응용하는 기술들 |

---

## 3. 7계층 프로토콜의 전송계층 매핑

| 프로토콜 | 전송계층 | 이유 |
|----------|----------|------|
| HTTP / HTTPS | TCP | 데이터 유실 안 되어야 함 |
| SSH | TCP | 신뢰성 필요 |
| SNMP | UDP | 빠르면 됨, 유실 감수 |
| Syslog | UDP | 로그 하나 빠져도 무방 |
| DNS | UDP (기본) | 빠른 응답 필요 |

> **핵심**: 4계층은 "어떻게 전달할지"(TCP/UDP), 7계층은 "전달된 데이터로 뭘 할지"(기능과 규칙)

---

## 4. 캡슐화 (Encapsulation)

데이터가 하위 계층으로 내려갈수록 헤더가 추가됨:

```
7~5계층:  [ DATA ]
4계층:    [ TCP Header | DATA ]                              → 세그먼트(Segment)
3계층:    [ IP Header | TCP Header | DATA ]                  → 패킷(Packet)
2계층:    [ MAC Header | IP | TCP | DATA | FCS ]             → 프레임(Frame)
1계층:    0101011010110101...                               → 비트(Bit)
```

---

## 5. 트러블슈팅 사고 틀

OSI를 배우는 진짜 이유 = **어느 계층에서 문제가 생겼는지 범위를 좁히는 사고 틀**

```
1계층 → 랜선 꽂혔어?       (링크 UP/DOWN)
2계층 → MAC 테이블 잡혔어?  (스위치 포워딩)
3계층 → ping 돼?           (ICMP 도달성)
4계층 → 포트 열렸어?        (TCP/UDP, 방화벽)
```

아래에서 위로 순서대로 올라가며 확인.

---

## 6. 알아둘 포인트

- **OSI 7계층은 이론적 참조 모델**, 실제 통신은 **TCP/IP 4계층**으로 동작
- **5·6계층은 사실상 개념적 분류**에 가까움 (실제 프로토콜은 TCP/IP 모델 기준 동작)
- **TCP/IP 4계층 매핑**:
  - 응용 계층 ← OSI 7·6·5
  - 전송 계층 ← OSI 4
  - 인터넷 계층 ← OSI 3
  - 네트워크 액세스 계층 ← OSI 2·1
- **NMS 실무 연결고리**: SNMP/Syslog가 UDP를 쓰는 이유 = 모니터링 패킷 하나 유실돼도 무방, 속도 우선

---

## 7. 암기법

> **"아 표 세 전 네 데 물"**
> **아**(응용) → **표**(표현) → **세**(세션) → **전**(전송) → **네**(네트워크) → **데**(데이터링크) → **물**(물리)
