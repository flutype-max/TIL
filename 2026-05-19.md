2026-05-19
[오늘 배운 것]

- WSL2 설치 및 Ubuntu 환경 세팅 (작업 본진)
- VirtualBox VM 3대 구성 + NAT/호스트전용 어댑터 네트워크 설정
- 클로드 코드 WSL2 설치 및 Anthropic 계정 로그인
- GitHub TIL 레포 생성 + 첫 커밋
- Ansible WSL2 설치 + inventory.ini 작성
- VM1, VM2 ping pong 연결 확인
- Nginx 자동 설치/설정 플레이북 작성 및 배포 성공
- ansible-vault로 슬랙 웹훅 URL 암호화 + group_vars/all.yml 저장
- 서비스 모니터링 + 슬랙 알림 플레이북 완성

[막혔던 부분]

- VM 인터넷 불가 (Could not resolve host): NAT 어댑터 순서가 어댑터1=호스트전용, 어댑터2=NAT로 반대로 되어 있었음
- netplan 설정에서 enp0s3 고정 IP 설정이 NAT와 충돌
- Ansible sudo 권한 오류: VM에서 NOPASSWD 설정 안 되어 있었음
- stopped_services 수집 로직 버그: .service 접미사 불일치 + inactive/stopped 상태 혼용

[해결 방법]

- VirtualBox에서 어댑터1을 NAT로 변경, enp0s3 netplan 설정 주석처리
- /etc/sudoers.d/test에 NOPASSWD 설정 추가
- 클로드 코드로 monitor_services.yml 버그 3개 수정 (facts에서 시작, regex_replace로 .service 제거, stopped/inactive 둘 다 포함)

[클로드 활용]

- 네트워크 트러블슈팅 단계별 가이드
- ansible-vault 사용법 및 group_vars 구조 설명
- 클로드 코드로 플레이북 버그 직접 수정

[내일 할 것]

- Ansible Role 구조 리팩토링 실습
- 포트폴리오 프로젝트 (ansible-network-monitor) 시작
