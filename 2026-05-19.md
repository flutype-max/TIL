# 2026-05-19 TIL

## 오늘 한 작업 전체

### 1. WSL2 환경 구축
- PowerShell 관리자 권한으로 `wsl --install` 실행
- Ubuntu 사용자 계정 생성 및 비밀번호 설정
- 기본 도구 설치: `git`, `curl`, `wget`, `vim`, `python3`, `python3-pip`, `python3-venv`
- Git 글로벌 설정 (user.name, user.email)

### 2. VirtualBox VM 3대 구성
- VM1, VM2, VM3 각각 Ubuntu Server 26.04 LTS 설치
- 각 VM 사양:
  - VM1, VM2: 2GB RAM / 20GB Disk (Ansible 타겟용)
  - VM3: 4GB RAM / 30GB Disk (Phase 4 AIOps 서비스용)
- OpenSSH server 설치 옵션 체크
- 사용자명: `test`로 통일

### 3. VM 네트워크 설정 (가장 많이 막혔던 부분)
- **초기 설정 실패 원인:**
  - 어댑터1=호스트전용, 어댑터2=NAT 순서로 되어있어서 인터넷 안 됨
  - `curl: (6) Could not resolve host: deb.nodesource.com` 에러 발생
- **해결 과정:**
  - `ip route show`로 default route 확인했지만 정상
  - `ping 10.0.2.2`도 안 나가서 NAT 자체 통신 불가 확인
  - 윈도우에서 `Get-NetAdapter`로 VirtualBox NAT 어댑터 없는 것 확인
  - VirtualBox 설정에서 **어댑터1을 NAT로 변경**
  - netplan 설정에서 `enp0s3` 블록을 주석 처리하여 NAT가 DHCP로 IP 자동 할당받도록 함
- **최종 IP 구성:**
  - VM1: 192.168.56.101 (호스트전용)
  - VM2: 192.168.56.102 (호스트전용)
  - VM3: 192.168.56.103 (호스트전용)
  - NAT는 각 VM에서 DHCP로 10.0.2.x 할당

### 4. SSH 키 기반 접속 설정
- WSL2에서 `ssh-keygen -t rsa -b 4096`로 키 생성
- `ssh-copy-id test@192.168.56.101~103`으로 각 VM에 공개키 등록
- 비밀번호 없이 접속 확인 완료

### 5. 클로드 코드 설치 및 로그인
- Node.js 20 설치 (`curl -fsSL https://deb.nodesource.com/setup_20.x | bash -`)
- `npm install -g @anthropic-ai/claude-code`로 클로드 코드 설치
- `claude` 명령으로 OAuth 로그인 (브라우저 URL 복사 방식)
- Claude Pro 계정 연결 확인

### 6. GitHub TIL 레포 생성
- GitHub 계정 생성
- `TIL` 레포 생성 (Public, README 포함)
- 첫 TIL 파일 `2026-05-19.md` 작성

### 7. Ansible 설치 및 인벤토리 설정
- WSL2에 `sudo apt install -y ansible` 설치 (컨트롤러는 WSL2, 타겟은 VM)
- `~/ansible-practice/` 작업 디렉토리 생성
- `inventory.ini` 작성:
```ini
  [targets]
  vm1 ansible_host=192.168.56.101 ansible_user=test
  vm2 ansible_host=192.168.56.102 ansible_user=test
```
- `ansible all -i inventory.ini -m ping` 성공 (pong 응답)

### 8. Nginx 자동 설치 플레이북 작성
- `playbooks/install_nginx.yml` 작성 (apt 업데이트 → nginx 설치 → 설정 배포 → 서비스 활성화)
- `templates/nginx.conf.j2` 작성 (Jinja2 템플릿, `{{ nginx_port }}` 변수 사용)
- **sudo 권한 에러 발생:** `sudo: interactive authentication is required`
- **해결:** 각 VM에 `echo "test ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/test` 적용
- 플레이북 실행 성공, VM1/VM2에서 403 응답 확인 (Nginx 정상 동작, index.html만 없는 상태)

### 9. ansible-vault로 시크릿 암호화
- Slack 워크스페이스 및 채널 생성
- `api.slack.com/apps`에서 Incoming Webhook 생성하여 웹훅 URL 발급
- `ansible-vault encrypt_string '웹훅URL' --name 'vault_slack_webhook'`로 암호화
- `group_vars/all.yml` 파일 생성하여 암호화된 변수 저장
- `group_vars/all.yml`은 Ansible이 자동으로 읽는 고정 경로임을 학습

### 10. 서비스 모니터링 + 슬랙 알림 플레이북
- `playbooks/monitor_services.yml` 작성
- nginx와 ssh 서비스 상태 체크
- 첫 실행 시 모든 서비스 정상이라 skip (정상 동작)
- **테스트:** vm1에서 `sudo systemctl stop nginx`로 nginx 강제 중단
- 재실행했으나 여전히 skipping → `stopped_services` 수집 로직 버그 발견

### 11. 클로드 코드로 플레이북 버그 수정
- `monitor_services.yml`의 `stopped_services` 수집 로직 3가지 버그:
  1. `services_to_check`에서 시작해서 키 불일치
  2. `ansible_facts.services` 키가 `nginx.service` 형태인데 `nginx`와 비교
  3. `inactive`만 잡고 `stopped` 상태 누락
- **수정 방향:**
  - `ansible_facts.services`에서 시작
  - `regex_replace('\.service$', '')`로 접미사 제거
  - `select('in', ['stopped', 'inactive'])`로 두 상태 모두 포함
- 수정 후 슬랙 알림 정상 수신 확인 🎉

## 막혔던 부분 정리

| 문제 | 원인 | 해결 |
|------|------|------|
| VM 인터넷 불가 | 어댑터 순서가 호스트전용/NAT 반대 | 어댑터1을 NAT로 변경 |
| netplan 충돌 | enp0s3 고정 IP가 NAT와 충돌 | enp0s3 설정 주석 처리 |
| Ansible sudo 에러 | NOPASSWD 미설정 | /etc/sudoers.d/test 설정 |
| 슬랙 알림 미전송 | stopped_services 로직 버그 | facts 기준으로 재작성 + .service 접미사 처리 |

## 클로드 활용 사례
- VirtualBox 네트워크 문제 단계별 진단 (ip route, ping 게이트웨이 순서로)
- ansible-vault 사용법 + group_vars 자동 로드 메커니즘 설명
- 클로드 코드로 monitor_services.yml 직접 수정 (Read → Update 흐름 경험)
- TIL 작성 보조

## 새로 배운 개념
- **WSL2의 역할:** 작업 본진 (Ansible 컨트롤러, 클로드 코드 등 모두 여기 설치)
- **idempotent:** 플레이북을 여러 번 실행해도 결과가 동일해야 한다는 Ansible 원칙
- **group_vars/all.yml:** Ansible이 자동으로 읽는 고정 경로, 모든 호스트에 변수 적용
- **host_vars/{호스트명}.yml:** 특정 호스트에만 변수 적용 (파일명이 inventory.ini의 호스트명과 정확히 일치해야 함)
- **Jinja2 템플릿(.j2):** 변수를 실제 값으로 치환해주는 템플릿 엔진
- **ansible-vault:** 민감 정보를 암호화하여 git에 안전하게 저장 가능

## 내일 할 것
- Ansible Role 구조 학습 및 기존 플레이북 리팩토링
- 슬랙 알림 메시지에서 nginx 중복 표시 문제 수정
- 포트폴리오 프로젝트 `ansible-network-monitor` 구조 잡기 시작
- NotebookLM에 오늘 TIL 업로드
